class Rectangle:
    def __init__(self, length, width):
        self.length = length
        self.width = width

    def __iter__(self):
        return iter([
            {'length': self.length},
            {'width': self.width}
        ])


Example:-
r = Rectangle(10, 5)

for i in r:
    print(i)


Output:-
{'length': 10}
{'width': 5}
